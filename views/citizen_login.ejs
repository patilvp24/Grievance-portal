<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CIVIC ACCESS PORTAL | Citizen Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700&family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #6e00ff;
            --primary-dark: #4d00b5;
            --secondary: #00f0ff;
            --accent: #ff00aa;
            --dark: #0a0a14;
            --dark-light: #1a1a2e;
            --light: #f0f4ff;
            --glass: rgba(255, 255, 255, 0.08);
            --glass-border: rgba(255, 255, 255, 0.15);
            --error: #ff3860;
        }
        
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        
        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, var(--dark), var(--dark-light));
            color: var(--light);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow: hidden;
            position: relative;
        }
        
        /* Holographic grid background */
        .grid-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                linear-gradient(rgba(0, 240, 255, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 240, 255, 0.03) 1px, transparent 1px);
            background-size: 40px 40px;
            animation: gridScroll 120s linear infinite;
            z-index: 0;
        }
        
        @keyframes gridScroll {
            0% { background-position: 0 0; }
            100% { background-position: 1000px 1000px; }
        }
        
        /* Floating cyber orbs */
        .cyber-orb {
            position: absolute;
            border-radius: 50%;
            filter: blur(60px);
            opacity: 0.15;
            animation: float 15s infinite ease-in-out;
        }
        
        .orb-1 {
            width: 300px;
            height: 300px;
            background: var(--primary);
            top: -50px;
            left: -50px;
        }
        
        .orb-2 {
            width: 400px;
            height: 400px;
            background: var(--accent);
            bottom: -100px;
            right: -100px;
            animation-delay: 3s;
        }
        
        @keyframes float {
            0%, 100% { transform: translate(0, 0); }
            50% { transform: translate(20px, 20px); }
        }
        
        /* Main container */
        .access-terminal {
            background: var(--glass);
            backdrop-filter: blur(16px);
            border: 1px solid var(--glass-border);
            border-radius: 16px;
            padding: 2.5rem;
            width: 90%;
            max-width: 450px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.3);
            position: relative;
            z-index: 2;
            overflow: hidden;
        }
        
        .access-terminal::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(
                circle, 
                rgba(110, 0, 255, 0.1) 0%, 
                rgba(255, 0, 170, 0.05) 70%, 
                transparent 100%
            );
            animation: rotate 60s linear infinite;
            z-index: -1;
        }
        
        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        /* Header section */
        .terminal-header {
            margin-bottom: 2rem;
            text-align: center;
            position: relative;
        }
        
        .terminal-header h1 {
            font-family: 'Orbitron', sans-serif;
            font-weight: 700;
            font-size: 1.8rem;
            color: transparent;
            background: linear-gradient(90deg, var(--secondary), var(--primary));
            -webkit-background-clip: text;
            background-clip: text;
            letter-spacing: 2px;
            margin-bottom: 0.5rem;
        }
        
        .access-level {
            display: inline-block;
            padding: 0.3rem 0.8rem;
            background: rgba(0, 240, 255, 0.2);
            border: 1px solid var(--secondary);
            border-radius: 20px;
            font-size: 0.7rem;
            font-family: 'Orbitron', sans-serif;
            color: var(--secondary);
            letter-spacing: 1px;
            margin-bottom: 1.5rem;
        }
        
        /* Form elements */
        .access-form {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }
        
        .input-group {
            position: relative;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            font-size: 0.85rem;
            color: var(--secondary);
            letter-spacing: 0.5px;
        }
        
        .input-field {
            position: relative;
        }
        
        .input-field i {
            position: absolute;
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--secondary);
            font-size: 1rem;
        }
        
        input {
            width: 100%;
            padding: 14px 14px 14px 44px;
            background: rgba(10, 10, 20, 0.6);
            border: 1px solid var(--glass-border);
            border-radius: 8px;
            font-size: 0.95rem;
            color: var(--light);
            transition: all 0.3s ease;
        }
        
        input:focus {
            outline: none;
            border-color: var(--secondary);
            box-shadow: 0 0 0 2px rgba(0, 240, 255, 0.2);
        }
        
        /* Buttons */
        .btn {
            width: 100%;
            padding: 16px;
            font-family: 'Orbitron', sans-serif;
            font-weight: 500;
            font-size: 0.95rem;
            letter-spacing: 1px;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            margin-top: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }
        
        .btn-primary {
            background: linear-gradient(90deg, var(--primary), var(--primary-dark));
            box-shadow: 0 4px 15px rgba(110, 0, 255, 0.3);
        }
        
        .btn-secondary {
            background: transparent;
            border: 1px solid var(--glass-border);
            color: var(--light);
        }
        
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(110, 0, 255, 0.4);
        }
        
        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(
                90deg,
                transparent,
                rgba(255, 255, 255, 0.2),
                transparent
            );
            transition: 0.5s;
        }
        
        .btn:hover::before {
            left: 100%;
        }
        
        .security-notice {
            margin-top: 25px;
            font-size: 0.7rem;
            color: rgba(255, 255, 255, 0.5);
            text-align: center;
            letter-spacing: 0.5px;
            line-height: 1.5;
        }
        
        .pulse {
            animation: pulse 3s infinite;
        }
        
        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(110, 0, 255, 0.5); }
            70% { box-shadow: 0 0 0 12px rgba(110, 0, 255, 0); }
            100% { box-shadow: 0 0 0 0 rgba(110, 0, 255, 0); }
        }
        
        /* Responsive adjustments */
        @media (max-width: 480px) {
            .access-terminal {
                padding: 1.8rem;
            }
            
            .terminal-header h1 {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="grid-bg"></div>
    <div class="cyber-orb orb-1"></div>
    <div class="cyber-orb orb-2"></div>
    
    <div class="access-terminal pulse">
        <div class="terminal-header">
            <h1>CIVIC ACCESS PORTAL</h1>
            <div class="access-level">CITIZEN VERIFICATION REQUIRED</div>
        </div>
        
        <form action="/citizen_login" method="post" class="access-form">
            <div class="input-group">
                <label for="email">CITIZEN ID</label>
                <div class="input-field">
                    <i class="fas fa-id-card"></i>
                    <input type="email" name="email" id="email" placeholder="Enter registered email" required>
                </div>
            </div>
            
            <div class="input-group">
                <label for="password">ACCESS CODE</label>
                <div class="input-field">
                    <i class="fas fa-lock"></i>
                    <input type="password" name="password" id="password" placeholder="Enter secure passcode" required>
                </div>
            </div>
            
            <button type="submit" class="btn btn-primary">
                <i class="fas fa-fingerprint"></i> VERIFY IDENTITY
            </button>
        </form>
        
        <form action="/citizen_signin" method="get" class="access-form">
            <button type="submit" class="btn btn-secondary">
                <i class="fas fa-user-plus"></i> REGISTER CITIZEN PROFILE
            </button>
        </form>
        
        <div class="security-notice">
            <i class="fas fa-shield-alt"></i> All credentials are encrypted with AES-256<br>
            Your civic data is protected under Digital Privacy Act
        </div>
    </div>
</body>
</html>